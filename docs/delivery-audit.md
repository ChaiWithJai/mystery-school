# Delivery audit

September 10, 2026. Product checkpoint: c8cd7dc. Scope: all three pathways,
the forest experience, Astra projection/correction, and inspectable provenance.
Agent checks support demo behavior, not learner efficacy.

## Issue 2

| Criterion | Current evidence |
| --- | --- |
| 1. Specific fictional characters | Each PATHS record retains desire, relationship, friction, voice and artifact. Labels remain visible in optional context. |
| 2. Complete circle for all three | Current GET-only `verify_pathways.mjs` passed all three saved chains through staged response, revision and next question. |
| 3. Different entry points | Audible envelope experiment, motion model, and sourced interpretation map share the same save/return infrastructure. |
| 4. Functioning change | Domain tests plus browser control/change/save/reopen checks for all three. |
| 5. Relevant help and ownership | Optional guidance, disagreement, draft retention, help-event links and stale-response regression tests. |
| 6. Domain grounding | Envelope/motion tests; source edition/section; separately labeled unverified video references. |
| 7. Care through action | Help returns to the chosen creation, permits refusal and does not claim spiritual authority. |
| 8. Response and next question | Staged responses are labeled; all three stored chains contain subsequent revision and question. |
| 9. Inspectable source records | Artifact parents, event IDs, model input snapshots, Studio links and actor provenance. Historical missing links remain disclosed. |
| 10. Capability boundaries | `verification.md` distinguishes implemented behavior, manual support, staged actions, agent QA and unresolved evidence. |

The six story steps are implemented in each path: care, try, needed concept,
made artifact, sharing response, and new question. Character context is optional
in the visual layout following the recorded presentation correction; it has not
been removed. The prepared `learner-pilot.md` covers fresh cases, expression and
support needs, assistance, facilitator effort and runtime accounting. Conducting
that subsequent study is not a demo acceptance requirement.

## Issue 1

| Criterion | Current evidence |
| --- | --- |
| 1. Synthetic review in MLflow and Studio | `synthetic-studio-check.json`; exact API/MLflow readback and browser-render verification. |
| 2. Retry without duplication | Roundtrip/conflict/reopen tests and identical import retries. No atomic batch API is claimed. |
| 3. Source and verification lineage | Exact reviewed commits, immutable finding/verification records, and browser navigation in both directions. |
| 4. Separate author and acceptor | Annotation/decision provenance tests, rewrite rejection, and actual agent-QA acceptance. Human branch tested synthetically only. |
| 5. Unknown versus supplied usage | Roundtrip fixture preserves reported usage/cost; absent fields remain null and are not aggregated as inference usage. |
| 6. Original versus import time | Source timestamps and import timestamps are separate; MLflow spans measure import, not original execution. |
| 7. Retryable ingestion failures | Export-failure, partial-export, readback-exception and readback-mismatch regressions retain IDs and withhold success until verified. |
| 8. Honest test provenance | Synthetic fixtures are labeled. Ingestion checks do not invoke models or upload private learner material. |

## Runtime evidence

`verify_demo.mjs`, `verify_capture.mjs`, `verify_learning_projection.mjs`, and
`verify_pathways.mjs` passed against the current local app during this audit.
The projection verifier compared immutable source, execution input, exact prompt,
result and review sample for every saved artifact-linked model job. It did not
make new model calls. Detailed IDs and limits are in `verification.md`.

The Blender forest and its saved-version markers remain part of the demo.
Generated scenarios change the existing scene and create saved branches; they
do not generate arbitrary new 3D geometry. Model usage is reported where supplied;
total build cost and unavailable sidecar inference telemetry remain unknown.

## Open gate

M4 supplied independent earlier tests, probes, and the merged visual redesign.
Main sent an explicit Buzz mention requesting its final acceptance ledger for
the integrated c8cd7dc build and confirmation of the runnable M4 checkout.
That final current-build acceptance is not yet observed. The overall goal remains
open; passing local tests do not replace the named independent gate.
